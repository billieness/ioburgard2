[build]
  base = "io-burgard"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Enable Git Gateway for CMS to commit to GitHub
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
